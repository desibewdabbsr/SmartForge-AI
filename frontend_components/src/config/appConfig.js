/**
 * Application Configuration
 */
const appConfig = {
  // Application name - can be replaced with dynamic machine name in the future
  appName: 'LocalMachine132',
  
  // Version information
  version: '1.0.0',
  
  // Other global settings
  settings: {
    // Add any other global settings here
  }
};

export default appConfig;